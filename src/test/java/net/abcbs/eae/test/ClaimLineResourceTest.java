package net.abcbs.eae.test;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import net.abcbs.eae.jaxrs.ClaimLineResource;

public class ClaimLineResourceTest {
	/**
	 * 
	 
    /**
     * Method under test: {@link ClaimLineResource#serviceLineMessage()}
     
    @Test
    public void testServiceLineMessage() {
        assertEquals(
                "{\"message\": \"This web service is designed to retrieve data values from Amisys databases. Please use"
                        + " claim number as the unique identifier during the request. \"}",
                (new ClaimLineResource()).serviceLineMessage());
    }

    /**
     * Method under test: {@link ClaimLineResource#testAuthorization()}
     
    @Test
    public void testTestAuthorization() {
        ClaimLineResource claimLineResource = new ClaimLineResource();
        claimLineResource.testAuthorization();
        assertEquals(
                "{\"message\": \"This web service is designed to retrieve data values from Amisys databases. Please use"
                        + " claim number as the unique identifier during the request. \"}",
                claimLineResource.serviceLineMessage());
    }
    
    **/
}

